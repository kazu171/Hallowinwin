# Frontend environment variables (do NOT commit real secrets)
# Replace placeholders with your actual values in your local .env
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Supabase CLI (optional, prefer interactive `supabase login`)
# Set in your shell, not in .env, when possible:
# SUPABASE_ACCESS_TOKEN= # NEVER commit a real token
# PROJECT_REF=YOUR_PROJECT_REF